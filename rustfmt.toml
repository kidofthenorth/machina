# Formatting is a CI gate. Keep this minimal and stable-only so `cargo fmt
# --check` works on the pinned stable toolchain without nightly options.
edition = "2021"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
