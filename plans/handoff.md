# Handoff — machina fresh-chat entry point (FOREMAN card loop)

**Role/scope.** This file seeds either seat. **PLANNER** (foreman, long-lived): audits reality,
writes cards, verifies every executor report (FOREMAN §6), stages, drafts commit messages, orders
reviews — never executes cards. **EXECUTOR** (one fresh chat per card): exactly one card, then
stop. Cadence: `docs/repo-kit/FOREMAN.md` · queue: `plans/task-queue.md` · status:
`plans/current-state.md` · log: `plans/worklog.md` (append-only) · roadmap: `plans/master-plan.md`.

## Verified baselines (2026-07-21 — generated by scripts/handoff-baselines.sh; re-verify in step 0, trust nothing you didn't re-run)
- machina HEAD: `a05880a feat(sweep): M-HF-C8.7d SweepReport additive HF extension…` · tree NOT
  clean: the C8.7e slice is STAGED awaiting operator commit (7 files: NEW hf_aggregate.rs +
  lib.rs + runner.rs + task-queue/worklog/current-state/handoff).
- gate (exit 0): **total: 485 passed; 0 failed; 1 ignored** · demo shasum
  `ae064f79242f823ffd8f55bf9104e3e1b45d425a` (×2 identical) · sweep shasum
  `f6e1ab5132754d69c3a2be23fc549df36c07909f` (fixed since C8.7d's sanctioned move) ·
  sweep-verify OK (18990 bytes) · no-exec-deps OK.
- Siblings (same pass): `../quant-sweep` @ `874d8eb`, clean, dormant. `../drift-harvester`
  (PLAN-001, the operator's other build) exists, branch main, ZERO commits, bootstrap staged —
  its own repo, never machina's concern.

## Mission state + exact next step
- M0–M5 DONE (M5 = reject-all 2026-07-12; holdout unread, seal intact). **M-HF track ACTIVE.**
  C1–C7, C8.1–C8.6b, and **C8.7a/b/c/d/e DONE + planner-verified** (worklog has per-card gate
  evidence; the sweep shasum moved exactly once, at C8.7d, per pinned decision D-a).
- The C8.7 sequence (task-queue.md §"M-HF-C8.7 planner reconciliation (2026-07-20)", pinned
  decisions D-a…D-h + Role rule): a✅ b✅ c✅ d✅ e✅ → **NEXT: ⛔ REVIEW-C8.4-SEAL** (the
  standing 7-lens FOREMAN §3 review of the C8.4 intraday holdout seal — **operator go-ahead
  REQUIRED**, multi-agent spend; lens list in the C8.6 reconciliation section; lens 7 gets
  drift item 3 + D-d/D-e; blocks C8.7f) → C8.7f (`run_hf_sweep` capstone, first real caller of
  the seal) → C8.7g (row-C8 gate battery; gate ignored-count goes 1→2 there, sanctioned).
  Then the post-C8.7 review before declaring.
- **Exact next step:** operator commits the staged C8.7e slice (message drafted this pass),
  then gives the go-ahead for ⛔ REVIEW-C8.4-SEAL. On "go", the PLANNER convenes the swarm
  (review-lens agents, `model: 'sonnet'`, low effort, report-only, skeptics + the §7
  pre-committed decision rule); confirmed blocker/major → STOP, finding becomes a card BEFORE
  C8.7f. No executor card runs until the review verdict is recorded.

## Hard rules that bite (full lists: AGENTS.md, docs/invariants.md)
- Agents never `git commit`/`push`; the operator commits. **One commit per card** (two-slice
  handovers failed twice — worklog 2026-07-21). Message = text only, never a `git commit`
  command; no AI/co-author trailers; diff every message against `git diff --cached --stat`.
- **Executors never draft commit messages or next-session prompts** (Role rule, task-queue.md).
- Never stage `.claude/` or `data/`; never edit `plans/master-plan.md`/
  `solana-crypto-trader-plan.md` (lockstep pair); no keys/signing/submit/network before M8/M9's
  separate explicit approval; determinism + fixed-point money non-negotiable; review swarms only
  with operator go-ahead, `model: 'sonnet'` low effort.
- GATE = `bash scripts/gate.sh` run unprompted, counts pasted never transcribed; handoff
  baselines only via `bash scripts/handoff-baselines.sh`.

## Staged/uncommitted + blockers
- STAGED (one slice, awaiting operator commit): the C8.7e card files
  (`crates/sweep/src/hf_aggregate.rs` NEW, `crates/sweep/src/lib.rs`,
  `crates/sweep/src/runner.rs`) + the four plan files (task-queue, worklog, current-state,
  handoff). Blockers: ⛔ REVIEW-C8.4-SEAL awaits operator go-ahead (blocks C8.7f). Deferred:
  M-HF-C8-PAIR (HF-Q2 `[[token]]` fields never supplied); hygiene F4 + CARD-HYG-2 open
  (D-0014).

## Seed — cast a fresh PLANNER (foreman seat)
> Read `docs/repo-kit/FOREMAN.md` — you are the **Planner** for **machina**, a paper-first Rust
> trading-research platform built toward **genuine autonomous passive income**, earned strictly
> through milestone gates; HF research on SYNTHETIC data only — no keys/signing/RPC/network.
> Step 0: run `bash scripts/gate.sh` and `git log -3`/`git status --porcelain` yourself and
> reconcile against `plans/handoff.md`'s baselines — prior summaries are not evidence. Then run
> the card loop from `plans/task-queue.md` (§M-HF-C8.7, decisions D-a…D-h): paste executor seeds
> one card per fresh chat; verify each report per FOREMAN §6 (re-run the gate yourself, check
> file scope, spot-check claims); stage card + pointer edits as ONE slice; draft ONE commit
> message (text only, no trailers, diffed against the staged stat); refresh handoff.md,
> current-state.md, and a dated worklog line each pass. **The next queue item is
> ⛔ REVIEW-C8.4-SEAL, not an executor card**: get the operator's go-ahead, then convene the
> 7-lens swarm (lens list in task-queue.md's C8.6 reconciliation section; review-lens agents,
> Sonnet low effort, report-only, skeptics per §7; feed lens 7 the C8.7 preamble's drift item 3
> + decisions D-d/D-e); record the verdict in the worklog; confirmed blocker/major → card
> BEFORE C8.7f. Never execute a card yourself; never declare on claims you haven't re-run.

## Next executor seed — NONE until the seal review verdict is recorded
The next executor card is **C8.7f** (`run_hf_sweep` capstone), but its escalate-if requires the
REVIEW-C8.4-SEAL verdict to exist first. The planner drafts the C8.7f seed only after the
review passes (or its findings are carded and resolved). Do not run C8.7f from this handoff.
