# Handoff — machina fresh-chat entry point (FOREMAN card loop)

**Role/scope.** This file seeds either seat. **PLANNER** (foreman, long-lived): audits reality,
writes cards, verifies every executor report (FOREMAN §6), stages, drafts commit messages, orders
reviews — never executes cards. **EXECUTOR** (one fresh chat per card): exactly one card, then
stop. Cadence: `docs/repo-kit/FOREMAN.md` · queue: `plans/task-queue.md` · status:
`plans/current-state.md` · log: `plans/worklog.md` (append-only) · roadmap: `plans/master-plan.md`.

## Verified baselines (2026-07-21 — generated by scripts/handoff-baselines.sh; re-verify in step 0, trust nothing you didn't re-run)
- machina HEAD: `e906607 feat(sweep): M-HF-C8.7e hf_aggregate…` · tree NOT clean: the
  REVIEW-C8.4-SEAL slice is STAGED awaiting operator commit (5 files: intraday_partition.rs
  test-only + task-queue/worklog/current-state/handoff).
- gate (exit 0): **total: 486 passed; 0 failed; 1 ignored** · demo shasum
  `ae064f79242f823ffd8f55bf9104e3e1b45d425a` (×2 identical) · sweep shasum
  `f6e1ab5132754d69c3a2be23fc549df36c07909f` (fixed since C8.7d's sanctioned move) ·
  sweep-verify OK (18990 bytes) · no-exec-deps OK.
- Siblings (same pass): `../quant-sweep` @ `874d8eb`, clean, dormant. `../drift-harvester`
  (PLAN-001, the operator's other build) exists, branch main, ZERO commits, bootstrap staged —
  its own repo, never machina's concern.

## Mission state + exact next step
- M0–M5 DONE (M5 = reject-all 2026-07-12; holdout unread, seal intact). **M-HF track ACTIVE.**
  C1–C7, C8.1–C8.6b, **C8.7a/b/c/d/e DONE + planner-verified**, and **REVIEW-C8.4-SEAL DONE
  2026-07-21: PASS-with-rulings, 0 blockers** (7 lenses + 8 skeptics; MAJOR carded as the
  M-HF-C8.8 stub — HF-priced holdout gateway, C9/C10 wave; one MINOR fixed as a ByDate
  degenerate-rejection test, which is why the gate count is 486 not 485; full record in the
  worklog).
- The C8.7 sequence (task-queue.md §"M-HF-C8.7 planner reconciliation (2026-07-20)", pinned
  decisions D-a…D-h + Role rule): a✅ b✅ c✅ d✅ e✅ REVIEW✅ → **NEXT: C8.7f**
  (`run_hf_sweep` capstone, first real caller of the seal — sanctioned ~200 lines, 2 files,
  BOTH shasums unchanged) → C8.7g (row-C8 gate battery; gate ignored-count goes 1→2 there,
  sanctioned). Then the post-C8.7 review (operator-gated) before row C8 is declared.
- **Exact next step:** operator commits the staged review slice (message drafted this pass),
  then pastes the C8.7f executor seed (below) into a fresh chat; planner verifies its report
  per §6 and stages card + pointer edits as ONE slice with ONE commit message.

## Hard rules that bite (full lists: AGENTS.md, docs/invariants.md)
- Agents never `git commit`/`push`; the operator commits. **One commit per card** (two-slice
  handovers failed twice — worklog 2026-07-21). Message = text only, never a `git commit`
  command; no AI/co-author trailers; diff every message against `git diff --cached --stat`.
- **Executors never draft commit messages or next-session prompts** (Role rule, task-queue.md).
- Never stage `.claude/` or `data/`; never edit `plans/master-plan.md`/
  `solana-crypto-trader-plan.md` (lockstep pair); no keys/signing/submit/network before M8/M9's
  separate explicit approval; determinism + fixed-point money non-negotiable; review swarms only
  with operator go-ahead, `model: 'sonnet'` low effort.
- GATE = `bash scripts/gate.sh` run unprompted, counts pasted never transcribed; handoff
  baselines only via `bash scripts/handoff-baselines.sh`.

## Staged/uncommitted + blockers
- STAGED (one slice, awaiting operator commit): `crates/sweep/src/intraday_partition.rs`
  (test-only §7 fix) + the four plan files (task-queue: REVIEW flip + C8.8 stub; worklog;
  current-state; handoff). Blockers: none for C8.7f. Deferred: M-HF-C8.8 (stub, C9/C10 wave);
  M-HF-C8-PAIR (HF-Q2 `[[token]]` fields never supplied); hygiene F4 + CARD-HYG-2 open
  (D-0014).

## Seed — cast a fresh PLANNER (foreman seat)
> Read `docs/repo-kit/FOREMAN.md` — you are the **Planner** for **machina**, a paper-first Rust
> trading-research platform built toward **genuine autonomous passive income**, earned strictly
> through milestone gates; HF research on SYNTHETIC data only — no keys/signing/RPC/network.
> Step 0: run `bash scripts/gate.sh` and `git log -3`/`git status --porcelain` yourself and
> reconcile against `plans/handoff.md`'s baselines — prior summaries are not evidence. Then run
> the card loop from `plans/task-queue.md` (§M-HF-C8.7, decisions D-a…D-h): paste executor seeds
> one card per fresh chat; verify each report per FOREMAN §6 (re-run the gate yourself, check
> file scope, spot-check claims); stage card + pointer edits as ONE slice; draft ONE commit
> message (text only, no trailers, diffed against the staged stat); refresh handoff.md,
> current-state.md, and a dated worklog line each pass. REVIEW-C8.4-SEAL is RULED
> (PASS-with-rulings, worklog 2026-07-21) — the queue now runs C8.7f → C8.7g, then the
> post-C8.7 review (operator go-ahead first). Never execute a card yourself; never declare on
> claims you haven't re-run.

## Seed — EXECUTOR on card M-HF-C8.7f (the next command)
> You are the EXECUTOR for **machina** — paper-first Rust trading research toward genuine
> autonomous passive income via milestone gates; SYNTHETIC data only, no keys/signing/RPC/
> network. **State (2026-07-21):** C1–C7, C8.1–C8.6b, C8.7a–e DONE; REVIEW-C8.4-SEAL RULED
> PASS (worklog 2026-07-21) so this card's review precondition is met; gate **486/0/1**; demo
> `ae064f79242f823ffd8f55bf9104e3e1b45d425a` (×2); sweep
> `f6e1ab5132754d69c3a2be23fc549df36c07909f`; sweep-verify OK. First act: run
> `bash scripts/gate.sh` and confirm these numbers.
> Your task is **card M-HF-C8.7f** in `plans/task-queue.md` (§"M-HF-C8.7 planner reconciliation
> (2026-07-20)"): `run_hf_sweep` — the windowed HF sweep capstone. Files (2): NEW
> `crates/sweep/src/hf_runner.rs`; `crates/sweep/src/lib.rs` (`pub mod hf_runner;` +
> `pub use hf_runner::{run_hf_sweep, HfSweepError, HfSweepOutcome};`). Sanctioned ~200 lines,
> single coherent orchestration. Read the section's common rules (incl. the Role rule) and
> decisions D-a…D-h, then follow the card's steps 1–5 EXACTLY: cell-id formulas verbatim
> (D-d, window index excluded; baseline tag bit); seal BEFORE any cell runs; regimes computed
> ONCE over dev-val and `.to_vec()` in BOTH closure arms (D-e); first-error-by-lowest-index
> collection; the seal returned UNCONSUMED in `HfSweepOutcome`.
> **This card moves NEITHER shasum** (a parallel path — it must not touch the LF sweep).
> `evaluate_intraday_on_holdout` is NEVER called — grep for it yourself before flipping.
> `runner.rs`/`spec.rs`/`cell.rs` byte-untouched. STOP if: any verbatim block mismatches; the
> closure cannot stay pure (do NOT add locks); either shasum moves.
> On green: flip C8.7f, one dated worklog line (counts + both shasums + the
> holdout-read-count-0 smoke evidence), stage exactly the card's 2 files + the two plan files,
> report, STOP.
> **Executors never draft commit messages or next-session prompts.** Never `git commit`/`push`;
> no trailers; never stage `.claude/`/`data/`; never touch `plans/master-plan.md`/
> `solana-crypto-trader-plan.md`; determinism and fixed-point money are non-negotiable.
